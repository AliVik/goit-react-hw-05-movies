import styled from 'styled-components';

export const Button = styled.button`
  display: block;
  cursor: pointer;
  padding-left: 60px;

  margin-bottom: 10px;
  border: none;
  background-color: transparent;
`;
